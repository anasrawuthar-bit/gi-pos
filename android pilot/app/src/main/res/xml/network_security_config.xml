<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- The Main PC server runs on the restaurant LAN and uses HTTP. -->
  <base-config cleartextTrafficPermitted="true" />
</network-security-config>
